{
  "_README": "BACKEND-ONLY settings for survey.py. This file now holds the OpenRouter API key (a SECRET). NEVER place this file in the web root (it must not be fetchable by the browser), and NEVER commit a real key to git. If set, the OPENROUTER_API_KEY environment variable overrides openrouter_api_key here.",

  "openrouter_api_key": "",
  "completion_code": "2026_LLM",

  "domain_name": "YOUR_DOMAIN.com",
  "port": 5000,
  "ssl_certfile": "/etc/letsencrypt/live/YOUR_DOMAIN.com/fullchain.pem",
  "ssl_keyfile": "/etc/letsencrypt/live/YOUR_DOMAIN.com/privkey.pem"
}
