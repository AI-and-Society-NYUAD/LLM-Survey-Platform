{
  "_README": "BACKEND-ONLY settings for survey.py. Do NOT place this file in the web root — it is not fetched by the browser. The completion_code is returned to participants only via /complete after they finish, so keep this file private. The OpenRouter API key is NOT here; set it as the OPENROUTER_API_KEY environment variable.",

  "completion_code": "2025_LLM",

  "domain_name": "YOUR_DOMAIN.com",
  "port": 5000,
  "ssl_certfile": "/etc/letsencrypt/live/YOUR_DOMAIN.com/fullchain.pem",
  "ssl_keyfile": "/etc/letsencrypt/live/YOUR_DOMAIN.com/privkey.pem"
}
